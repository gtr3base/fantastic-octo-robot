spring:
  application:
    name: BCardManagingSys
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: hypadJ
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: "bank_card_schema"
        format_sql: true
      open-in-view: false
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml
    drop-first: false

logging:
  level:
    com:
      gtr3base:
        BCardManagingSys:
          security: TRACE

BCardManagingSys:
  jwt:
    secret: "3ecf5388e220da9e0f919485deb676d8bee3aec046a779353b463418511ee622"
    expires-in-ms: 360000


springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

server:
  port: 8084